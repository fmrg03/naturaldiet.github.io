const NavBar = () => {
    return (
        <nav>
            <a href="#">Nosotros</a>
            <a href="#">Productos</a>
            <a href="#">Contacto</a>
        </nav>
    )
};

export default NavBar;